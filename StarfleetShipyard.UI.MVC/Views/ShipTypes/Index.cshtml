@model IEnumerable<StarfleetShipyard.DATA.EF.Models.ShipType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ShipTypesName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShipTypesDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShipTypesName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShipTypesDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ShipTypesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ShipTypesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ShipTypesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
